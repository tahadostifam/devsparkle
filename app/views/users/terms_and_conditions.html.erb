<div class="container" style="top: 0px">
    <h2 class="container_title bb">قوانین وبسایت</h2>

    <% if @setting.present? %>
        <div class="content md_to_html" style="margin: 10px 0;" dir="rtl">
            <%= @setting.tac %>
        </div>
    <% end %>
</div>
