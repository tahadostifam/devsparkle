<div class="content">
		<h2 class="container_title bb">دوره های من (<%= @my_courses.length %>)</h2>
		<% if @my_courses.length > 0 %>
		<div class="listed_cards mini_list">
		    <% @my_courses.each do |co| %>
                <%= render 'courses/course_card', co: co, show_publish_banner: true %>
			<% end %>
		</div>
		<% else %>
			<span style='color: #777; font-size: 14px; width: 100%; display: block; text-align: center; padding: 20px 0'>دوره ای پیدا نشد.</span>
		<% end %>
    </div>